machine:
  environment:
    DATABASE_URL: 'sqlite://:memory:'
    DJANGO_SETTINGS_MODULE: codefarmert.settings.test

deployment:
  staging:
    branch: master
    heroku:
      appname: codefarmert-staging

